import { Fullpage, Slide, HorizontalSlider } from 'fullpage-react';

const fullPageOptions = {
    scrollsensitivity = 7,
    touchSensitivity = 8,
    scrollSpeed = 500,
    hideScrollbars = true,
    enableArrowKeys = true
};